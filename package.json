{
  "name": "real-time-fund",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "daemon": "node src/cli.js daemon",
    "daemon:once": "node src/cli.js daemon:once",
    "config:validate": "node src/cli.js config:validate",
    "state": "node src/cli.js state",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepare": "husky"
  },
  "dependencies": {
    "dayjs": "^1.11.19"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "vitest": "^3.2.4"
  }
}
